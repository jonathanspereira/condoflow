version: '3.8'

services:
  condoflow-db:
    image: postgres:16-alpine
    container_name: condoflow_postgres
    restart: always
    environment:
      POSTGRES_DB: condoflow_db
      POSTGRES_USER: condoflow_user
      POSTGRES_PASSWORD: condoflow_password
    ports:
      - "5432:5432"
    volumes:
      - condoflow-data:/var/lib/postgresql/data

volumes:
  condoflow-data: